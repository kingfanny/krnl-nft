{
  "name": "krnl-nft",
  "devDependencies": {
    "solhint": "^5.0.5"
  },
  "scripts": {
    "lint": "solhint 'src/**/*.sol' 'script/**/*.sol' 'test/**/*.sol'",
    "lint:fix": "solhint --fix 'src/**/*.sol' 'script/**/*.sol' 'test/**/*.sol'"
  }
}
